import React from 'react';

export const ColdWarChartsPageSimple: React.FC = () => {
  return (
    <div
      style={{
        background: '#0a0a0c',
        color: '#f0a000',
        padding: '40px',
        minHeight: '100vh',
        fontFamily: 'monospace',
      }}
    >
      <h1>✅ COLD WAR CHARTS - WORKING!</h1>
      <p>If you see this page, the routing is working correctly.</p>

      <div style={{ marginTop: '40px', padding: '20px', border: '2px solid #f0a000' }}>
        <h2>Test Chart 1</h2>
        <canvas
          width={400}
          height={300}
          style={{ background: '#1a1a1c', border: '1px solid #f0a000' }}
        />
      </div>

      <div style={{ marginTop: '40px', padding: '20px', border: '2px solid #f0a000' }}>
        <h2>Test Chart 2</h2>
        <canvas
          width={400}
          height={300}
          style={{ background: '#1a1a1c', border: '1px solid #f0a000' }}
        />
      </div>
    </div>
  );
};

export default ColdWarChartsPageSimple;
