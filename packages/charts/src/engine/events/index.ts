/**
 * Event System
 * Handles chart events and interactions
 */

import { EventSystem } from './EventSystem';

export { EventSystem, type ChartEvent, type EventHandler } from './EventSystem';

export default {
  EventSystem,
};
